import styled from "styled-components";

export default styled.div`
    
        display: flex;
        align-items: center;
    
    .random, .mode {
        color: white;
        font-size: 10px;
        margin-left: 20px;
    }
`